# Google Maps API Key (geocoding do MapPicker; será migrado)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# Drop Maps — chave do MapTiler (vector tiles). PÚBLICA (vai pro browser):
# restrinja por domínio no painel do MapTiler (dropapp.com.br, localhost).
NEXT_PUBLIC_MAPTILER_KEY=
